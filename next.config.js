/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['brandpalettes.com']
  },
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
